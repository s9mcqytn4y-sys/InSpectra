package com.primaraya.inspectra.core.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary (Deep Blue) ──
val BluePrimary = Color(0xFF3B82F6)       // Blue 500
val BluePrimaryDark = Color(0xFF1D4ED8)   // Blue 700
val BlueContainer = Color(0xFF1E3A5F)     // Muted blue container
val OnBlueContainer = Color(0xFFBFDBFE)   // Blue 200

// ── Secondary (Amber) ──
val AmberAccent = Color(0xFFF59E0B)       // Amber 500
val AmberDark = Color(0xFFD97706)         // Amber 600
val AmberContainer = Color(0xFF451A03)    // Amber muted dark
val OnAmberContainer = Color(0xFFFDE68A)  // Amber 200

// ── Tertiary (Emerald) ──
val EmeraldSuccess = Color(0xFF10B981)    // Emerald 500
val EmeraldContainer = Color(0xFF064E3B)  // Emerald 900
val OnEmeraldContainer = Color(0xFFA7F3D0) // Emerald 200

// ── Surfaces (Slate) ──
val SlateBg = Color(0xFF020617)           // Slate 950 — deepest background
val SlateBase = Color(0xFF0F172A)         // Slate 900 — primary surface
val SlateCard = Color(0xFF1E293B)         // Slate 800 — card/container
val SlateElevated = Color(0xFF334155)     // Slate 700 — elevated surface
val SlateOutline = Color(0xFF475569)      // Slate 600 — border/outline
val SlateSubtle = Color(0xFF64748B)       // Slate 500 — secondary text
val SlateText = Color(0xFF94A3B8)         // Slate 400 — body text
val SlateBright = Color(0xFFCBD5E1)       // Slate 300 — primary text on dark
val SlateWhite = Color(0xFFF1F5F9)        // Slate 100 — brightest text

// ── Status ──
val StatusOk = Color(0xFF22C55E)          // Green 500
val StatusNg = Color(0xFFEF4444)          // Red 500
val StatusPending = Color(0xFFF59E0B)     // Amber 500
val StatusNgContainer = Color(0xFF450A0A) // Red muted dark
val OnStatusNg = Color(0xFFFCA5A5)        // Red 300

// ── Light Mode Fallback ──
val LightBg = Color(0xFFF8FAFC)
val LightSurface = Color.White
val LightCard = Color(0xFFF1F5F9)
val LightTextPrimary = Color(0xFF0F172A)
val LightTextSecondary = Color(0xFF64748B)
val LightOutline = Color(0xFFE2E8F0)
